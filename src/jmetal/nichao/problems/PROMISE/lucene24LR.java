package jmetal.nichao.problems.PROMISE;

import jmetal.nichao.problems.BaseProblemImpl;

public class lucene24LR extends BaseProblemImpl {
	public lucene24LR(String solutionType) {
		//只需要更改此处的事先就可以完整所有方法的实现了
		super(solutionType,"lucene24LR","weka.classifiers.functions.Logistic");
	}
}
